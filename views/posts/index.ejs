LISTA DE POSTS

<% posts.forEach((post) => { %>
    <div class="post">
        <hr>
        <h3><%=post.id%> - <%=post.title%></h3>
        <p><%=post.body%></p>
        <a href="/posts/<%=post._id%>">Veja o post</a>
        <hr>
    </div>
<% }) %>